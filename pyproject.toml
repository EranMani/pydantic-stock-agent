[project]
name = "stock-agent"
version = "0.1.0"
description = "Autonomous PydanticAI Stock Analyst Agent"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stock_agent"]
