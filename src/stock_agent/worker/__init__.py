"""Celery worker package for the stock analyst agent.

Contains the Celery application instance (celery_app.py), the Redis progress
publisher (state.py), and all background task definitions (tasks.py).
"""
